# v0.5.0: publication archive update

Adds the bounded tone-count screen and four-tone packet records; finite-size planar-wall magnetic comparisons with aligned carriers and atomic-receiver alternatives; the digitized historical Macnae background and declared low-frequency fits; and equal-mass array illustration records. The previously qualified seven-tone designs remain unchanged.

Four-tone statistical qualification is separate from detector convergence. Wider-detector replays of error-containing packets remain outstanding; the archived screening and qualification reports retain this limitation. Electromagnetic comparisons use aligned 24/24/18 Hz centers and numerical power ceilings equal to gravity peak mechanical powers, not measured equal average electrical powers. The older cavity geometry and freely optimized sub-Hz results are historical/auxiliary only.

No manuscript drafts, cover letters, coauthor correspondence, login information, or third-party source screenshot are included. The Macnae trace is cited as a historical schematic, not a measured contemporary site spectrum. Supply the original image separately to regenerate its visual trace overlay. CSV anchors and the SI interpolation model are included.

Reproduction entry points (run from repository root with PYTHONPATH=src):
- python scripts/finalize_tone_count_study.py
- python scripts/magnetic_wall_aligned_carriers.py
- python scripts/magnetic_wall_atomic_receiver.py
- python scripts/plot_em_gravity_noise.py
- python scripts/range_array_comparison.py

Historical qualification source hashes and snapshots are retained. The archive manifest hashes the distributed bytes. Machine-specific paths in result metadata are replaced with relative paths; this is not a change to numerical data.

Validation: 147 unit tests passed. All 66,167 data-manifest hashes verified; four-tone summaries were independently recomputed from all 26,240 archived packet records.
